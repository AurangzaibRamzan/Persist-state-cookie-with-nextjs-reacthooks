# Persist-state-cookie-with-nextjs-reacthooks

example of persist Cookie  data using react hooks in Next js

### just play with it

- `npm i`

- `npm run-script dev`

🚀  ready on http://localhost:3000
